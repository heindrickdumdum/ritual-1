{%- style -%}
  #shopify-section-{{ section.id }} {
    --section-padding-top: {{ section.settings.padding-block-start }}px;
    --section-padding-bottom: {{ section.settings.padding-block-end }}px;
    --section-padding-top-mobile: {{ section.settings.padding-block-start-mobile }}px;
    --section-padding-bottom-mobile: {{ section.settings.padding-block-end-mobile }}px;
  }

  #shopify-section-{{ section.id }} .take-quiz {
    padding-top: var(--section-padding-top);
    padding-bottom: var(--section-padding-bottom);
  }

  #shopify-section-{{ section.id }} .take-quiz__container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 5rem;
    align-items: center;
  }

  #shopify-section-{{ section.id }} .take-quiz__content {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  #shopify-section-{{ section.id }} .take-quiz__title {
    font-weight: 500;
    margin: 0;
    color: #181D27;
    line-height: 136%;
    letter-spacing: -0.02em;
  }

  {% if section.settings.title_font_size != blank %}
  #shopify-section-{{ section.id }} .take-quiz__title {
    font-size: {{ section.settings.title_font_size }}px;
  }
  {% endif %}

  #shopify-section-{{ section.id }} .take-quiz__description {
    margin: 0;
    color: #535862;
    line-height: 140%;
  }

  {% if section.settings.description_font_size != blank %}
  #shopify-section-{{ section.id }} .take-quiz__description {
    font-size: {{ section.settings.description_font_size }}px;
  }
  {% endif %}

  #shopify-section-{{ section.id }} .take-quiz__timeline {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  #shopify-section-{{ section.id }} .take-quiz__step {
    display: flex;
    gap: 2rem;
    position: relative;
  }

  #shopify-section-{{ section.id }} .take-quiz__step:not(:last-child)::after {
    content: '';
    position: absolute;
    left: 24px;
    top: 70px;
    width: 2px;
    height: 100%;
    background: #E5E5E0;
  }

  #shopify-section-{{ section.id }} .take-quiz__step-circle {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
    font-size: 1.5rem;
    flex-shrink: 0;
    position: relative;
    z-index: 1;
  }

  #shopify-section-{{ section.id }} .take-quiz__step--active .take-quiz__step-circle {
    background: #181D27;
    color: white;
  }

  #shopify-section-{{ section.id }} .take-quiz__step--inactive .take-quiz__step-circle {
    background: #ffffff;
    border: 1px solid #D5D7DA;
    color: #A4A7AE;
  }

  #shopify-section-{{ section.id }} .take-quiz__step-content {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    padding-top: 0.5rem;
    width: 100%;
  }

  #shopify-section-{{ section.id }} .take-quiz__step.take-quiz__step--inactive {
    align-items: center;
  }

  #shopify-section-{{ section.id }} .take-quiz__step-content {
    padding-top: 0;
  }

  #shopify-section-{{ section.id }} .take-quiz__step-buttons {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  #shopify-section-{{ section.id }} .take-quiz__step-text {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  #shopify-section-{{ section.id }} .take-quiz__step-title {
    margin: 0;
    color: #181D27;
    font-weight: 700;
    font-size: 24px;
    line-height: 140%;
  }

  #shopify-section-{{ section.id }} .take-quiz__step--inactive .take-quiz__step-title {
    color: #A4A7AE;
    font-size: 18px;
    line-height: 145%;
  }

  #shopify-section-{{ section.id }} .take-quiz__step-subtitle {
    margin: 0;
    color: #535862;
    font-size: 20px;
    line-height: 140%;
  }

  #shopify-section-{{ section.id }} .take-quiz__button {
    padding: 1.25rem 2rem;
    background: #26736D;
    color: white;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    transition: background-color 0.3s ease;
    text-decoration: none;
    display: inline-block;
    text-align: center;
    width: 100%;
    font-weight: 700;
    font-size: 18px;
    line-height: 28px;
  }

  #shopify-section-{{ section.id }} .take-quiz__button:hover {
    background-color: #1f5952;
  }

  #shopify-section-{{ section.id }} .take-quiz__button-meta {
    color: #535862;
    text-align: center;
    font-weight: 450;
    font-size: 16px;
    line-height: 150%;
    display: flex;
    align-items: center;
    gap: 8px;
    justify-content: center;
  }

  /* Tablet Styles */
  @media (max-width: 989px) {
    #shopify-section-{{ section.id }} .take-quiz__container {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    #shopify-section-{{ section.id }} .take-quiz__title {
      font-size: 2rem;
    }

    #shopify-section-{{ section.id }} .take-quiz__step {
      gap: 1.5rem;
    }
  }

  /* Mobile Styles */
  @media (max-width: 749px) {
    #shopify-section-{{ section.id }} {
      --section-padding-top: var(--section-padding-top-mobile);
      --section-padding-bottom: var(--section-padding-bottom-mobile);
    }

    #shopify-section-{{ section.id }} .take-quiz__title {
      font-size: 1.75rem;
    }

    {% if section.settings.title_font_size_mobile != blank %}
    #shopify-section-{{ section.id }} .take-quiz__title {
      font-size: {{ section.settings.title_font_size_mobile }}px;
    }
    {% endif %}

    #shopify-section-{{ section.id }} .take-quiz__description {
      font-size: 0.95rem;
    }

    #shopify-section-{{ section.id }} .take-quiz__step-circle {
      width: 45px;
      height: 45px;
      font-size: 1.1rem;
    }

    #shopify-section-{{ section.id }} .take-quiz__step-title {
      font-size: 1.1rem;
    }
  }
{%- endstyle -%}

<div class="take-quiz section section--page-width color-{{ section.settings.color_scheme }}" style="padding-top: var(--section-padding-top); padding-bottom: var(--section-padding-bottom);">
  <div class="container">
    <div class="take-quiz__container">
      {%- if section.settings.title != blank or section.settings.description != blank -%}
        <div class="take-quiz__content">
          {%- if section.settings.title != blank -%}
            <h2 class="take-quiz__title">{{ section.settings.title }}</h2>
          {%- endif -%}
          {%- if section.settings.description != blank -%}
            <p class="take-quiz__description">{{ section.settings.description }}</p>
          {%- endif -%}
        </div>
      {%- endif -%}

      <div class="take-quiz__timeline">
        {%- for block in section.blocks -%}
          {%- if block.type == 'step' -%}
            <div class="take-quiz__step take-quiz__step--{% if block.settings.is_active %}active{% else %}inactive{% endif %}" {{ block.shopify_attributes }}>
              <div class="take-quiz__step-circle">{{ forloop.index }}</div>
              
              <div class="take-quiz__step-content">
                <div class="take-quiz__step-text">
                  {%- if block.settings.title != blank -%}
                    <h3 class="take-quiz__step-title">{{ block.settings.title }}</h3>
                  {%- endif -%}

                  {%- if block.settings.subtitle != blank -%}
                    <p class="take-quiz__step-subtitle">{{ block.settings.subtitle }}</p>
                  {%- endif -%}
                </div>

                {%- if block.settings.is_active -%}
                  <div class="take-quiz__step-buttons">
                    {%- if block.settings.button_text != blank and block.settings.button_url != blank -%}
                      <a href="{{ block.settings.button_url }}" class="take-quiz__button">{{ block.settings.button_text }}</a>
                    {%- endif -%}

                    {%- if block.settings.button_meta != blank -%}
                      <div class="take-quiz__button-meta">
                        <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
                          <path d="M9.99996 18.3332C14.6023 18.3332 18.3333 14.6022 18.3333 9.99984C18.3333 5.39746 14.6023 1.6665 9.99996 1.6665C5.39759 1.6665 1.66663 5.39746 1.66663 9.99984C1.66663 14.6022 5.39759 18.3332 9.99996 18.3332Z" stroke="#535862" stroke-width="1.5"/>
                          <path d="M10 6.6665V9.99984L11.6667 11.6665" stroke="#535862" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>

                        {{ block.settings.button_meta }}
                      </div>
                    {%- endif -%}
                  </div>
                {%- endif -%}
              </div>
            </div>
          {%- endif -%}
        {%- endfor -%}
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Take Quiz",
  "tag": "section",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Unlock Your Personalized Hair Growth Plan"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Stop wasting time on products that don't deliver. We create a tailored hair growth routine using clinically backed ingredients and scalp-focused strategies — built specifically for your hair type and goals."
    },
    {
      "type": "header",
      "content": "Typography"
    },
    {
      "type": "range",
      "id": "title_font_size",
      "label": "Title font size",
      "min": 24,
      "max": 80,
      "step": 1,
      "unit": "px",
      "default": 44
    },
    {
      "type": "range",
      "id": "title_font_size_mobile",
      "label": "Title font size (mobile)",
      "min": 16,
      "max": 60,
      "step": 1,
      "unit": "px",
      "default": 28
    },
    {
      "type": "range",
      "id": "description_font_size",
      "label": "Description font size",
      "min": 12,
      "max": 24,
      "step": 1,
      "unit": "px",
      "default": 16
    },
    {
      "type": "header",
      "content": "Section Padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Top padding",
      "min": 0,
      "max": 200,
      "step": 5,
      "unit": "px",
      "default": 100
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Bottom padding",
      "min": 0,
      "max": 200,
      "step": 5,
      "unit": "px",
      "default": 100
    },
    {
      "type": "range",
      "id": "padding-block-start-mobile",
      "label": "Top padding (mobile)",
      "min": 0,
      "max": 200,
      "step": 5,
      "unit": "px",
      "default": 65
    },
    {
      "type": "range",
      "id": "padding-block-end-mobile",
      "label": "Bottom padding (mobile)",
      "min": 0,
      "max": 200,
      "step": 5,
      "unit": "px",
      "default": 65
    }
  ],
  "blocks": [
    {
      "type": "step",
      "name": "Step",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "placeholder": "e.g., Take the Hair Quiz"
        },
        {
          "type": "text",
          "id": "subtitle",
          "label": "Subtitle",
          "placeholder": "e.g., Take the Hair Quiz"
        },
        {
          "type": "checkbox",
          "id": "is_active",
          "label": "Is this an active step?",
          "default": false
        },
        {
          "type": "text",
          "id": "button_text",
          "label": "Button text",
          "placeholder": "e.g., Take The Quiz"
        },
        {
          "type": "url",
          "id": "button_url",
          "label": "Button link"
        },
        {
          "type": "text",
          "id": "button_meta",
          "label": "Button meta text",
          "placeholder": "e.g., Only takes 60 seconds"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Take Quiz",
      "blocks": [
        {
          "type": "step",
          "settings": {
            "title": "Take the Hair Quiz",
            "subtitle": "Take the Hair Quiz",
            "is_active": true,
            "button_text": "Take The Quiz",
            "button_meta": "Only takes 60 seconds"
          }
        },
        {
          "type": "step",
          "settings": {
            "title": "Here's your personalized hair growth routine...",
            "is_active": false
          }
        }
      ]
    }
  ]
}
{% endschema %}
