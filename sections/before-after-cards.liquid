{{ 'overflow-list.css' | asset_url | stylesheet_tag }}

{%- style -%}
  #shopify-section-{{ section.id }} {
    --section-padding-top: {{ section.settings.padding-block-start }}px;
    --section-padding-bottom: {{ section.settings.padding-block-end }}px;
  }

  #shopify-section-{{ section.id }} .section {
    padding-top: var(--section-padding-top);
    padding-bottom: var(--section-padding-bottom);
  }

  #shopify-section-{{ section.id }} .before-after-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  #shopify-section-{{ section.id }} .before-after-header__title {
    font-weight: 400;
    margin: 0;
    color: rgb(var(--color-foreground));
  }

  #shopify-section-{{ section.id }} .before-after-carousel {
    position: relative;
    margin-bottom: 3rem;
  }

  #shopify-section-{{ section.id }} .before-after-carousel__wrapper {
    overflow: hidden;
    margin: 0 60px;
  }

  @media (max-width: 749px) {
    #shopify-section-{{ section.id }} .before-after-carousel__wrapper {
      margin: 0 40px;
    }
  }

  #shopify-section-{{ section.id }} .before-after-carousel__track {
    display: flex;
    gap: 1.5rem;
    transition: transform 0.3s ease;
  }

  #shopify-section-{{ section.id }} .before-after-card {
    flex: 0 0 300px;
    background: #F7F7F5;
    border-radius: 16px;
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
  }

  @media (max-width: 749px) {
    #shopify-section-{{ section.id }} .before-after-card {
      flex: 0 0 260px;
    }
  }

  #shopify-section-{{ section.id }} .before-after-card__images {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 1rem;
  }

  #shopify-section-{{ section.id }} .before-after-card__image-wrapper {
    flex: 1;
    position: relative;
    aspect-ratio: 1;
    border-radius: 12px;
    overflow: hidden;
    background: #E5E5E0;
  }

  #shopify-section-{{ section.id }} .before-after-card__image-wrapper img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  #shopify-section-{{ section.id }} .before-after-card__image-label {
    position: absolute;
    bottom: 0.5rem;
    left: 50%;
    transform: translateX(-50%);
    background: rgba(255, 255, 255, 0.9);
    padding: 0.25rem 0.75rem;
    border-radius: 12px;
    font-size: 0.75rem;
    color: rgb(var(--color-foreground));
    font-weight: 500;
  }

  #shopify-section-{{ section.id }} .before-after-card__title {
    font-size: 1.125rem;
    font-weight: 500;
    margin: 0 0 0.75rem;
    color: rgb(var(--color-foreground));
  }

  #shopify-section-{{ section.id }} .before-after-card__details {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  #shopify-section-{{ section.id }} .before-after-card__detail {
    display: flex;
    justify-content: space-between;
    font-size: 0.875rem;
  }

  #shopify-section-{{ section.id }} .before-after-card__detail-label {
    color: rgba(var(--color-foreground), 0.6);
  }

  #shopify-section-{{ section.id }} .before-after-card__detail-value {
    color: rgb(var(--color-foreground));
    font-weight: 500;
  }

  #shopify-section-{{ section.id }} .before-after-carousel__nav {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: #4A9B8E;
    border: none;
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: opacity 0.3s ease;
    z-index: 2;
  }

  #shopify-section-{{ section.id }} .before-after-carousel__nav:hover {
    opacity: 0.9;
  }

  #shopify-section-{{ section.id }} .before-after-carousel__nav:disabled {
    opacity: 0.3;
    cursor: not-allowed;
    background: #F7F7F4;
    border: 1px solid #D5D7DA;
    color: #181D27;
  }

  #shopify-section-{{ section.id }} .before-after-carousel__nav--prev {
    left: 0;
  }

  #shopify-section-{{ section.id }} .before-after-carousel__nav--next {
    right: 0;
  }

  #shopify-section-{{ section.id }} .before-after-carousel__nav svg {
    width: 24px;
    height: 24px;
  }

  #shopify-section-{{ section.id }} .before-after-carousel__progress {
    height: 2px;
    background: #E5E5E0;
    border-radius: 1px;
    overflow: hidden;
    max-width: 1192px;
    /* margin: 0 auto 2rem; */
    margin: 0 auto;
  }

  #shopify-section-{{ section.id }} .before-after-carousel__progress-bar {
    height: 100%;
    background: #181D27;
    transition: width 0.3s ease;
  }

  #shopify-section-{{ section.id }} .before-after-cta {
    text-align: center;
  }

  #shopify-section-{{ section.id }} .before-after-cta__button {
    display: inline-block;
    background: #4A9B8E;
    color: white;
    padding: 1rem 2rem;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 500;
    transition: opacity 0.3s ease;
  }

  #shopify-section-{{ section.id }} .before-after-cta__button:hover {
    opacity: 0.9;
  }
{%- endstyle -%}

<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<div class="section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }}">
  <div class="container">
    {%- if section.settings.title != blank -%}
      <div class="before-after-header">
        <h2 class="before-after-header__title">{{ section.settings.title }}</h2>
      </div>
    {%- endif -%}

    <div class="before-after-carousel" data-carousel>
      <div class="before-after-carousel__wrapper">
        <div class="before-after-carousel__track" data-carousel-track>
          {%- for block in section.blocks -%}
            {%- if block.type == 'testimonial_card' -%}
              <article class="before-after-card" {{ block.shopify_attributes }}>
                <div class="before-after-card__images">
                  <div class="before-after-card__image-wrapper">
                    {%- if block.settings.before_image != blank -%}
                      {{
                        block.settings.before_image
                        | image_url: width: 400
                        | image_tag:
                          loading: 'lazy',
                          width: block.settings.before_image.width,
                          height: block.settings.before_image.height,
                          alt: 'Before'
                      }}
                    {%- endif -%}
                    <span class="before-after-card__image-label">Before</span>
                  </div>
                  <div class="before-after-card__image-wrapper">
                    {%- if block.settings.after_image != blank -%}
                      {{
                        block.settings.after_image
                        | image_url: width: 400
                        | image_tag:
                          loading: 'lazy',
                          width: block.settings.after_image.width,
                          height: block.settings.after_image.height,
                          alt: 'After'
                      }}
                    {%- endif -%}
                    <span class="before-after-card__image-label">After</span>
                  </div>
                </div>

                {%- if block.settings.title != blank -%}
                  <h3 class="before-after-card__title">{{ block.settings.title }}</h3>
                {%- endif -%}

                <div class="before-after-card__details">
                  {%- if block.settings.treatment != blank -%}
                    <div class="before-after-card__detail">
                      <span class="before-after-card__detail-label">Treatment</span>
                      <span class="before-after-card__detail-value">{{ block.settings.treatment }}</span>
                    </div>
                  {%- endif -%}

                  {%- if block.settings.duration != blank -%}
                    <div class="before-after-card__detail">
                      <span class="before-after-card__detail-label">Duration</span>
                      <span class="before-after-card__detail-value">{{ block.settings.duration }}</span>
                    </div>
                  {%- endif -%}
                </div>
              </article>
            {%- endif -%}
          {%- endfor -%}
        </div>
      </div>

      <div class="tw-py-8">
        <div class="tw-flex tw-items-center tw-relative tw-mt-8 tw-max-w-[1360px] tw-mx-auto">
        <button class="before-after-carousel__nav before-after-carousel__nav--prev" data-carousel-prev aria-label="Previous">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
          </svg>
        </button>

        <button class="before-after-carousel__nav before-after-carousel__nav--next" data-carousel-next aria-label="Next">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </button>

        <div class="before-after-carousel__progress tw-w-full">
          <div class="before-after-carousel__progress-bar" data-carousel-progress></div>
        </div>
      </div>
      </div>
    </div>

    {%- if section.settings.button_text != blank and section.settings.button_url != blank -%}
      <div class="before-after-cta">
        <a href="{{ section.settings.button_url }}" class="before-after-cta__button">
          {{ section.settings.button_text }}
        </a>
      </div>
    {%- endif -%}
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const carousels = document.querySelectorAll('[data-carousel]');
    
    carousels.forEach(carousel => {
      const track = carousel.querySelector('[data-carousel-track]');
      const prevBtn = carousel.querySelector('[data-carousel-prev]');
      const nextBtn = carousel.querySelector('[data-carousel-next]');
      const progressBar = carousel.querySelector('[data-carousel-progress]');
      const cards = track.querySelectorAll('.before-after-card');
      
      let currentIndex = 0;
      const cardWidth = 300 + 24; // card width + gap
      const visibleCards = Math.floor(carousel.offsetWidth / cardWidth);
      const maxIndex = Math.max(0, cards.length - visibleCards);

      function updateCarousel() {
        const offset = currentIndex * cardWidth;
        track.style.transform = `translateX(-${offset}px)`;
        
        // Update progress bar
        const progress = maxIndex > 0 ? (currentIndex / maxIndex) * 100 : 100;
        progressBar.style.width = `${progress}%`;
        
        // Update button states
        prevBtn.disabled = currentIndex === 0;
        nextBtn.disabled = currentIndex >= maxIndex;
      }

      prevBtn.addEventListener('click', () => {
        if (currentIndex > 0) {
          currentIndex--;
          updateCarousel();
        }
      });

      nextBtn.addEventListener('click', () => {
        if (currentIndex < maxIndex) {
          currentIndex++;
          updateCarousel();
        }
      });

      updateCarousel();

      // Update on resize
      window.addEventListener('resize', () => {
        currentIndex = 0;
        updateCarousel();
      });
    });
  });
</script>

{% schema %}
{
  "name": "Before After Cards",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Results You'll Love - Here's the Proof"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button text",
      "default": "Start Your Free Consultation"
    },
    {
      "type": "url",
      "id": "button_url",
      "label": "Button link"
    },
    {
      "type": "header",
      "content": "t:content.section_layout"
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "t:settings.width",
      "options": [
        {
          "value": "page-width",
          "label": "t:options.page"
        },
        {
          "value": "full-width",
          "label": "t:options.full"
        }
      ],
      "default": "page-width"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:settings.color_scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:content.padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "t:settings.top",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "t:settings.bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 48
    }
  ],
  "blocks": [
    {
      "type": "testimonial_card",
      "name": "Testimonial Card",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Name/Title",
          "default": "Customer's Story"
        },
        {
          "type": "image_picker",
          "id": "before_image",
          "label": "Before image"
        },
        {
          "type": "image_picker",
          "id": "after_image",
          "label": "After image"
        },
        {
          "type": "text",
          "id": "treatment",
          "label": "Treatment",
          "default": "Treatment name"
        },
        {
          "type": "text",
          "id": "duration",
          "label": "Duration",
          "default": "3 months"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Before After Cards",
      "blocks": [
        {
          "type": "testimonial_card",
          "settings": {
            "title": "Lina's Transformation",
            "treatment": "Clear Complexion",
            "duration": "4 months"
          }
        },
        {
          "type": "testimonial_card",
          "settings": {
            "title": "Tom's Revival",
            "treatment": "Hydration Boost",
            "duration": "3 months"
          }
        },
        {
          "type": "testimonial_card",
          "settings": {
            "title": "Sophia's Renewal",
            "treatment": "Radiant Glow",
            "duration": "5 months"
          }
        },
        {
          "type": "testimonial_card",
          "settings": {
            "title": "Jake's Clarity",
            "treatment": "Pore Refinement",
            "duration": "6 months"
          }
        }
      ]
    }
  ]
}
{% endschema %}
